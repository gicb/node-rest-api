// Jenkinsfile (Declarative Pipeline)
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh "pwd"
                sh "sleep(5)"
                sh "ls -la"
            }
        }
    }
}